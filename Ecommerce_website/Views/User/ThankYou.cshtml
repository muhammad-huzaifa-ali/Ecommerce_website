
<div class="container py-5">

    <div class="thank-card">

       

        <div class="mb-4">
            <img src="https://cdn-icons-png.flaticon.com/512/845/845646.png"
                 width="90">
        </div>
        <h1 class="fw-bold text-success">
            Order Successful 🎉
        </h1>

        <p class="fs-5 text-muted mb-4">
            Thank you for shopping with <b>Techora</b> ❤️<br>
            Your order has been placed successfully.
        </p>

        <a href="/User/Index" class="btn-shop mt-4 d-inline-block">
            Continue Shopping
        </a>

    </div>

</div>


<style>
    body {
        background: linear-gradient(135deg,#f6f7fb,#e9ecf5);
    }

    .thank-card {
        max-width: 550px;
        margin: auto;
        background: #fff;
        border-radius: 20px;
        padding: 50px;
        box-shadow: 0 20px 60px rgba(0,0,0,0.15);
        text-align: center;
        /* animation */
        transform: scale(0.5) translateY(80px);
        opacity: 0;
        animation: popup 0.6s ease forwards;
    }

    @@keyframes popup {
        0% {
            transform: scale(0.5) translateY(80px);
            opacity: 0;
        }

        60% {
            transform: scale(1.05) translateY(-10px);
            opacity: 1;
        }

        100% {
            transform: scale(1) translateY(0);
            opacity: 1;
        }
    }

    .tick {
        font-size: 70px;
        color: #28a745;
        margin-bottom: 15px;
    }

    .btn-shop {
        background: linear-gradient(45deg,#ffb300,#ff8800);
        color: #fff;
        font-weight: bold;
        padding: 14px 35px;
        border-radius: 30px;
        border: none;
        transition: 0.3s;
        text-decoration: none;
    }

        .btn-shop:hover {
            transform: scale(1.08);
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
            color: #fff;
        }

    @@keyframes tickpop {
        to {
            opacity: 1;
            transform: scale(1);
        }
    }
</style>
